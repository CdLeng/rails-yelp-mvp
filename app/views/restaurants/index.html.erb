<h2>Restaurants</h2>
<ul>
  <% @restaurants.each do |restaurant| %>
    <li><%= link_to(restaurant.name, show_restaurant_path(restaurant)) %></li>
  <% end %>
</ul>
<%= link_to('Add a restaurant', create_restaurant_path)%>
